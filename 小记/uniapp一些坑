坑
1.页面生命周期只能在页面中使用。
2.页面之间可用uni.$emit监听也用传值navegato,监听注意关闭，有时候在组件中监听不好使，因为不能在onlond中触发，要在created中，
监听要记得关闭，直接用uni.off（"方法名"）
3.golbdata如果没值会不执行，但页面不会报错，手机端运行记得把console.log注释。切记。
4.页面返回最好用back返回，
好处：
1. 可以保存页面数据。返回不丢失。
2。输入框不可以输入0和两个点
